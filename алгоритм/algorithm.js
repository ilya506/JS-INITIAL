function first(numb){
    var number = numb;
    if(Math.floor(number / 2) == number / 2) {
        //Чётное
        return false;
    }else{
        //Нечётное
        return true;
    }
}

function first(numb){
    var number = numb;
    if(Math.floor(number / 5) == number / 5) {
        //Чётное
        return false;
    }else{
        //Нечётное
        return true;
    }
}

function first(numb){
    var number = numb;
    if(Math.floor(number / 8) == number /8) {
        //Чётное
        return false;
    }else{
        //Нечётное
        return true;
    }
}
